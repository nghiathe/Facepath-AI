# Mốc 1 — scaffold + seed. Chưa cần embeddings/LLM (mốc 5).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
PyMySQL>=1.1,<2.0          # thuần Python: không phải build, an toàn trên Windows + Python 3.13
python-dotenv>=1.0,<2.0
